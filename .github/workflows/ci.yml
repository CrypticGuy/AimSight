name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  DOTNET_VERSION: '9.0.x'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore ${{ github.workspace }}/CrossHairPlus.sln

      - name: Build
        run: dotnet build ${{ github.workspace }}/CrossHairPlus.sln -c Release --no-restore

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: |
            CrossHairPlus/bin/Release/net9.0-windows/win-x64/**
            OverlayHook/bin/Release/**/OverlayHook.dll
          retention-days: 7
