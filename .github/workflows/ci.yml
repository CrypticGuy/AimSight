name: CI

on:
  push:
    branches: [ main, develop, master ]
  pull_request:
    branches: [ main, master ]

env:
  DOTNET_VERSION: '8.0.x'

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore ${{ github.workspace }}/CrossHairPlus.sln -r win-x64

      - name: Build
        run: dotnet build ${{ github.workspace }}/CrossHairPlus.sln -c Release -r win-x64 --no-restore

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-output
          path: CrossHairPlus/bin/Release/net8.0-windows/win-x64/**
          retention-days: 7
